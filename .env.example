# Database Configuration
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=football_analytics

# Host Configuration
# Default to 'localhost' for local tooling (poetry run, alembic)
# Docker Compose will override this to 'db' automatically
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
